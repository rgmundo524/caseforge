# urls

Example OSINT feature overlay.

This is a safe placeholder feature:
- it adds an Evidence page
- it adds a source-query surface with the intended schema
- it does **not** require a live OSINT ingestor yet

Replace `sources/case/url_indicators.sql` with a real table/view query when your
URL collection pipeline exists.
