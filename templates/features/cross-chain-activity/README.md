# cross-chain-activity

Example feature overlay that adds a focused cross-chain analysis page and
two feature-specific source-query surfaces.

This feature assumes the base case database already includes:
- `v_cross_chain_pairs`
- `v_cross_chain_tx_legs`
