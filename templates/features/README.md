# Feature overlays

Feature overlays are optional layers applied **after** the primary template.

Use them for capability add-ons, not for whole case-type ownership.

Recommended pattern:
- primary template owns the main site structure
- features add pages, source queries, and helper queries with unique paths

Examples in this folder:
- `cross-chain-activity`
- `urls`
